# TEST-ONLY private material

This directory contains deterministic fixture material used solely to reproduce the synthetic HIE source-payload commitment. It is intentionally distributed for scientific reproducibility, is not confidential and must never be reused as an operational nonce, credential or key.

The public distribution audit allowlists only this README and `payload_commitment_nonce.hex` within the directory.
