# Reproducibility

The kit is self-contained for Python checks. It requires Python, pytest and Hypothesis. The bounded executable model is finite-scope and enumerates generated append sequences; it is not a complete formal proof.
