# C3 integrity synchronisation trigger

This controlled marker triggered regeneration of `FILE_MANIFEST.tsv` and `SHA256SUMS.txt` after the retained C3 evidence, build-residue exclusions, standards-status update, warning adjudication and claim-ledger update were integrated.

The marker is not scientific evidence. The authoritative evidence remains under `data_examples/hie_disclosure/` and `standards/fhir_ig/validation/`.

Temporary verification trigger; removed in the immediately following commit.
