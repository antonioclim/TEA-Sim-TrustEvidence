# C3 integrity synchronisation trigger

This controlled marker triggered regeneration of `FILE_MANIFEST.tsv` and `SHA256SUMS.txt` after the retained C3 evidence, build-residue exclusions, standards-status update, warning adjudication, claim-ledger update and final C3 audit documents were integrated.

The marker is not scientific evidence. The authoritative evidence remains under `data_examples/hie_disclosure/` and `standards/fhir_ig/validation/`.

Temporary verification marker; removed in the following commit.
