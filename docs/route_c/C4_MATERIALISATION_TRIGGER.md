# C4 evidence materialisation trigger

This controlled marker requests one hosted execution of the frozen Route C C4 security and mutation programme. The workflow must retain the deterministic result files, update the result-level reproducibility manifest and rebuild the repository integrity manifests.

The marker is not scientific evidence. Authoritative C4 evidence is the generated corpus under `results_expected/cmpb_reference/c4_hie_security/`, its hosted execution transcript and the subsequent all-green release contract.
