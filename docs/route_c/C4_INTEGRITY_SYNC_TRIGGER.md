# C4 integrity synchronisation trigger

This controlled marker requests regeneration of `FILE_MANIFEST.tsv` and `SHA256SUMS.txt` after the C4 result corpus, protocol-deviation record, claim ledgers, hostile audit and removal of temporary materialisation machinery were integrated.

The marker is not scientific evidence. The authoritative evidence is the retained C4 corpus and its hosted verification records.
