# C5 integrity synchronisation trigger

This controlled marker requests regeneration of `FILE_MANIFEST.tsv` and `SHA256SUMS.txt` after the C5 evidence, reviewer registries, claim controls and temporary-workflow removal have been integrated.

The marker is governance metadata, not scientific evidence.
