# FHIR base-release decision

The draft TrustEvidence implementation-guide material uses FHIR R4 because IHE BALP is R4-based and AuditEvent/Provenance mappings are most immediately testable against that ecosystem. Future versions may add an R5 mapping once the R4 draft compiles cleanly and the target standards pathway is agreed.
