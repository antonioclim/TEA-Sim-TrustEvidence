# Figure source data

This directory contains the CSV inputs used by `figures/scripts/generate_jcis_figures.py`. Conceptual figures use explicit node/edge/tile data files; quantitative annotations use the evidence CSV extracts copied from the FHIR/BALP, backend, workload and property-validation kits. The generator writes SVG, PDF and 300 dpi PNG outputs.
