# Expected quick local reference outputs

These files were generated from `python experiments/run_experiments.py --quick` during archive construction. Timing fields are environment-specific and are supplied as schema/format exemplars, not as production performance estimates.
